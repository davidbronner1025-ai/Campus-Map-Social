## 2024-05-15 - Add ARIA Labels to Notification Bell Buttons\n**Learning:** Icon-only buttons for critical UI elements like notifications must have screen-reader accessible names using `aria-label`, especially interactive elements like "mark all read" and the main notification toggle.\n**Action:** Always verify that icon-only interactive components have an accessible `aria-label`.
