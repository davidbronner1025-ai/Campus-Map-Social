## 2024-05-20 - English vs Hebrew Accessibility in NotificationBell
**Learning:** While the primary language of the application might be Hebrew, some components like NotificationBell use English for their text content.
**Action:** When adding ARIA labels, ensure the language matches the component's visible text (e.g., using "Notifications" instead of Hebrew translations if the component's UI text is in English).
